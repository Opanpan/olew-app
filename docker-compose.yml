version: '3.8'

services:
  welo-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: welo-group-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development

  welo-prod:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: welo-group-prod
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
